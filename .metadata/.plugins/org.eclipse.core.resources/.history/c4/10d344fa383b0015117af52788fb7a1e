from django.shortcuts import render

# Create your views here.

def cc_manual(request):
    
    # title, num, multi
    return render(request, 'manual/main.html')


def cl_manual(request):
    
    # title, num, multi
    return render(request, 'manual/main.html')